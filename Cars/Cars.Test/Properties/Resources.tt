//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//     Runtime Version:4.0.30319.34003
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
<#@ template hostspecific="true" language="C#" #>
<#@ output extension=".Designer.cs" #>
<#@ assembly name="System.Xml" #>
<#@ assembly name="System.Xml.Linq" #>
<#@ import namespace="System.IO" #>
<#@ import namespace="System.Xml.Linq" #>

namespace Cars.Test.Properties
{
    using System;
    using System.ComponentModel;
    using System.Globalization;
    using System.Resources;
    using System.Threading;
    using Cars.I18n;

    public class Resources
    {
        private static IResourceManager resourceManager;
        private static CultureInfo resourceCulture;

        [EditorBrowsableAttribute(EditorBrowsableState.Advanced)]
        public static IResourceManager ResourceManager
        {
            get
            {
                if(resourceManager == null)
                {
                    IResourceManager temp = new ResourceManagerWrapper(new ResourceManager("Cars.Test.Properties.Resources", typeof(Resources).Assembly));
                    resourceManager = temp;
                }

                return resourceManager;
            }

            set
            {
                resourceManager = value;
            }
        }

        [EditorBrowsableAttribute(EditorBrowsableState.Advanced)]
        public static CultureInfo Culture
        {
            get
            {
                return resourceCulture;
            }

            set
            {
                resourceCulture = value;
            }
        }
<#
    string resxFileName = this.Host.TemplateFile.Replace(".tt", ".resx");
    XDocument doc = XDocument.Load(resxFileName);

    if(doc != null && doc.Root != null)
    {
        foreach(XElement x in doc.Root.Descendants("data"))
        {
            string name = x.Attribute("name").Value;
            string type = x.Attribute("type") != null ? x.Attribute("type").Value : string.Empty;

            if(string.IsNullOrEmpty(type))
            {
                WriteLine(string.Empty);
                WriteLine("        public static string " + name);
                WriteLine("        {");
                WriteLine("            get { return Resources.ResourceManager.GetString(\"" + name + "\", resourceCulture ?? CultureInfo.CurrentUICulture); }");
                WriteLine("        }");
            }
            else if(string.Equals("System.Resources.ResXFileRef, System.Windows.Forms", type, StringComparison.Ordinal))
            {
                string value = (string)x.Element("value");
                if(!string.IsNullOrWhiteSpace(value) && value.IndexOf("System.String, mscorlib", StringComparison.Ordinal) > -1)
                {
                    WriteLine(string.Empty);
                    WriteLine("        public static string " + name);
                    WriteLine("        {");
                    WriteLine("            get { return Resources.ResourceManager.GetString(\"" + name + "\", resourceCulture ?? CultureInfo.CurrentUICulture); }");
                    WriteLine("        }");
                }
                else
                {
                    WriteLine(string.Empty);
                    WriteLine("        public static byte[] " + name);
                    WriteLine("        {");
                    WriteLine("            get");
                    WriteLine("            {");
                    WriteLine("                object obj = ResourceManager.GetObject(\"" + name +"\", resourceCulture ?? CultureInfo.CurrentUICulture);");
                    WriteLine("                return ((byte[])(obj));");
                    WriteLine("            }");
                    WriteLine("        }");
                }
            }
        }
    }
#>
    }
}