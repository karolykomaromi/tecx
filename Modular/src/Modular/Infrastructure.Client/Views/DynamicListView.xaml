<UserControl x:Class="Infrastructure.Views.DynamicListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Infrastructure.ViewModels"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400"
    d:DataContext="{d:DesignInstance d:Type=viewModels:DynamicListViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" CornerRadius="5" Margin="10" BorderThickness="1" Background="GreenYellow">
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" />
        </Border>
        <sdk:DataGrid Grid.Row="1" ItemsSource="{Binding Items}" />
    </Grid>
</UserControl>
