<UserControl x:Class="Infrastructure.NavigationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:mvvm="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.SL5"
    xmlns:inf="clr-namespace:Infrastructure"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400"
    d:DataContext="{d:DesignInstance d:Type=inf:NavigationViewModel}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseLeftButtonDown">
            <mvvm:EventToCommand Command="{Binding NavigationCommand}" CommandParameter="{Binding Destination}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Border Background="LightBlue" CornerRadius="20" BorderBrush="Black" BorderThickness="1" Margin="5">
        <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold" Margin="10" TextAlignment="Center" />
    </Border>
</UserControl>
